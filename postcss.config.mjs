/** @type {import('postcss-load-config').Config} */
  plugins: {
    tailwindcss: {},
  },
};

export default config;

const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
